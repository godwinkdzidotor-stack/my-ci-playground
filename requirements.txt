Flask==0.12.0             # VERY old, multiple known CVEs (High)
Werkzeug==0.16.0          # Multiple medium/high vulns
urllib3==1.24.1           # HIGH, CRITICAL CVEs known
itsdangerous==0.24        # Very old, known issues
click==6.7                 # Old version
pytest==3.3.0             # Outdated, contains vulnerable deps
